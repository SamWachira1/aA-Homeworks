<h1>Cat's Rule the World</h1>
<ul>
<% @cat.all.each do |cat|%>
   <li>
         <a href="<%= cat_url(cat) %>"><%= cat.name %></a>

         <form action="<%= cat_url(cat) %>%" method= "POST">
            <input type="hidden" name="_method" value="DELETE"> 

            <input type="submit" value= "DESTROY CAT">
         </form>
   </li>
 <%end %>
</ul>

<a href="<%= new_cat_url %>">New Cat!</a>